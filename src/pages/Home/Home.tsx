import React from 'react';
import Layout from '../../layout/Layout';

type Props = {};

const Home = (props: Props) => {
  return (
    <Layout>
      <h1>Boi</h1>
    </Layout>
  );
};

export default Home;
